import Foundation

/// Copy this file to `Secrets.swift` for local development, or set
/// `ANTHROPIC_API_KEY` in CI to generate it automatically.
enum Secrets {
    static let anthropicAPIKey = ""
}
